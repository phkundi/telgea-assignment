# Password that gates the whole app (build-time, Vite exposes VITE_-prefixed vars).
# Copy this file to `.env.local` and set a value. If left empty, the gate is disabled.
# Note: a static front-end has no backend, so this password ships in the client
# bundle — it is a soft gate for casual access, not real security.
VITE_APP_PASSWORD=
